export default function Header() {
  return (
    <header className="sticky top-0 z-40 h-16 border-b border-black/10 bg-brand-primary/95 backdrop-blur">
      <div className="mx-auto flex h-full w-full max-w-[1400px] items-center px-4 sm:px-6 lg:px-8">
        <h1 className="text-xl font-semibold tracking-wide text-white">Visualizar Calhas</h1>
      </div>
    </header>
  );
}
